diff --git a/node_modules/@astrojs/sitemap/dist/index.js b/node_modules/@astrojs/sitemap/dist/index.js
index 248b1fc..930008d 100644
--- a/node_modules/@astrojs/sitemap/dist/index.js
+++ b/node_modules/@astrojs/sitemap/dist/index.js
@@ -82,7 +82,12 @@ const createPlugin = (options) => {
               }
             }
           };
-          const routeUrls = _routes.reduce((urls, r) => {
+          if (!_routes) {
+            logger.warn(
+              "astro:routes:resolved never fired before astro:build:done -- sitemap will only include page URLs, not dynamic route URLs (patched via patch-package, see AGENT-HANDOFF.md)."
+            );
+          }
+          const routeUrls = (_routes ?? []).reduce((urls, r) => {
             if (r.type !== "page") return urls;
             addRouteUrl(urls, r);
             for (const fallbackRoute of r.fallbackRoutes ?? []) {
