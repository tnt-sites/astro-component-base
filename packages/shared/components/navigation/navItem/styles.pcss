.nav-item {
  position: relative;
}

.nav-item > a {
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: var(--space-xs);
}

.nav-item-toggle {
  display: none;
}

.nav-item-trigger {
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: var(--space-xs);
}
