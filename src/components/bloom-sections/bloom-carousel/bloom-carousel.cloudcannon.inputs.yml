backgroundColor:
  type: select
  comment: Background color used behind the grid.
  options:
    values:
      - id: none
        name: None
      - id: base
        name: Base
      - id: surface
        name: Surface
      - id: accent
        name: Accent
      - id: highlight
        name: Highlight
carouselSlides:
  type: array
  options:
    structures:
      values:
        - label: Carousel Slide
          value:
            _component: bloom-carousel/carousel-slide
            waveImageSource:
            waveImageAlt:
            imageSource:
            imageAlt:
            heading:
            text:
# Add your other component inputs here
# Example:
# text:
#   type: text
#   comment: The text content for the component.