---
import { navListSchema } from "../nav-item-list-schema";
import "./nav-list.pcss";

const props = navListSchema.parse(Astro.props);
---

<ul
  class:list={["nav-list", props.layout, props.className]}
  id={props.id}
  style={props.customStyle}
  role="menu"
>
  <slot />
</ul>
