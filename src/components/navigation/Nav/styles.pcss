.nav {
  --nav-width: 100%;
  width: var(--nav-width);
}

.nav.sidebar {
  --nav-width: 250px;

  > .inner {
    flex-direction: column;
    align-items: flex-start;
    justify-content: flex-start;
  }
}

::slotted([slot="brand"]) {
  flex-shrink: 0;
  display: flex;
}
