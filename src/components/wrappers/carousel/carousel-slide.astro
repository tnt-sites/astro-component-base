---
import Component from "@components/utils/renderBlock.astro";

const { contentBlocks = [], ...rest } = Astro.props;

const htmlAttributes = Object.fromEntries(
  Object.entries(rest).filter(([key]) => key !== "_bookshop_name")
);
---

<div class="slide" role="group" {...htmlAttributes}>
  <Component contentBlocks={contentBlocks} />
  <slot />
</div>
