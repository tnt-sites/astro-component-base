---
import clsx from "clsx";
import "./definition-list.pcss";

const {
  items,
  horizontalAlignment = "start",
  class: className,
  ...restProps
} = Astro.props;

const htmlAttributes = Object.fromEntries(
  Object.entries(restProps).filter(([key]) => !key.startsWith("_"))
);

const classNames = clsx("definition-list", `alignment-${horizontalAlignment}`, className);
---

<dl class:list={classNames} {...htmlAttributes}>
  {
    items.map((item: any) => {
      return (
        <dt class="title" role="term"><strong>{item.title}</strong></dt>
        <dd class="content" role="definition">{item.content}</dd>
      );
    })
  }
</dl>
