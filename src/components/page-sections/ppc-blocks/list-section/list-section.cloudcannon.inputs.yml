backgroundColor:
  type: select
  comment: Background color used behind the grid.
  options:
    values:
      - id: none
        name: None
      - id: base
        name: Base
      - id: surface
        name: Surface
      - id: accent
        name: Accent
      - id: highlight
        name: Highlight
items:
  type: array
  comment: Blocks for each image + heading + text
  item:
    type: object
    properties:
      headingPrefix:
        type: text
        comment: First part of heading (e.g., "Looking for a")
      headingMain:
        type: text
        comment: Main heading (e.g., "New Dentist?")
      desktopImage:
        type: image
        comment: Desktop image URL
      mobileImage:
        type: image
        comment: Mobile image URL
      alt:
        type: text
        comment: Alt text for images
      listText:
        type: text
        comment: Text below images
# Add your other component inputs here
# Example:
# text:
#   type: text
#   comment: The text content for the component.
