heading:
  type: text
  comment: Section heading.

images:
  type: array
  comment: Before/after or smile result images.
  options:
    structures:
      values:
        - label: Result Image
          icon: image
          value:
            source: ""
            alt: ""
          _inputs:
            source:
              type: image
              options:
                paths:
                  uploads: src/assets/images
                  static: ""
            alt:
              type: text

backgroundColor:
  type: select
  comment: Background color of the section. Choose a theme color or use the hex override below for a custom color.
  options:
    values:
      - id: ""
        name: Default (White)
      - id: "#fff"
        name: White
      - id: "#000"
        name: Black
      - id: "var(--color-brand)"
        name: Brand (Primary)
      - id: "var(--color-brand-secondary)"
        name: Brand (Secondary)
      - id: "var(--color-text)"
        name: Text

backgroundColorHex:
  type: color
  comment: Optional custom hex color that overrides the background color above. Leave blank to use the theme color.

textColor:
  type: color
  comment: Heading text color.
