spec:
  structures:
    - contentBlocks
    - firstContentBlocks
    - secondContentBlocks
  label: Definition List
  description: "For displaying key-value pairs. Ideal for glossaries, opening times, etc."
  icon: dns
  tags: []

blueprint:
  items:
  horizontalAlignment: start

preview:

_inputs:
  items:
    type: array
    label: List Items
    options:
      structures: _structures.item
  horizontalAlignment:
    type: select
    options:
      values:
        - id: start
          name: Start
        - id: center
          name: Center
        - id: end
          name: End

_structures:
  item:
    values:
      - label: Definition List Item
        value:
          _component: definition-list-item
          title:
          text:
        _inputs:
          title:
            type: text
            comment: A bolded title for the list item.
            options:
              required: true
              required_message: List item must contain a title.
          text:
            type: textarea
            comment: The text content of the list item.
        preview:
          icon: list
          text:
            - key: title
            - Definition List Item
          subtext:
            - key: text
