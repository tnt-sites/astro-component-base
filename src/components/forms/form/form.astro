---
import RenderBlock from "@components/utils/renderBlock.astro";

const { action, formBlocks = [], class: className, ...rest } = Astro.props;

const htmlAttributes = Object.fromEntries(
  Object.entries(rest).filter(([key]) => key !== "_bookshop_name")
);
---

<form
  class:list={["form", className]}
  action={action}
  method="post"
  enctype="text/plain"
  {...htmlAttributes}
>
  <RenderBlock contentBlocks={formBlocks} />
  <slot />
</form>
