---
const { value = "", label = "", selected = false, disabled = false, ...rest } = Astro.props;

const htmlAttributes = Object.fromEntries(
  Object.entries(rest).filter(([key]) => key !== "_bookshop_name")
);
---

<option value={value} selected={selected} disabled={disabled} {...htmlAttributes}>
  {label}
  <slot />
</option>
