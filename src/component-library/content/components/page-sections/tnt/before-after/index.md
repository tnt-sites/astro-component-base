---
title: Before & After
description: "Before/after image-pair comparison for treatment, renovation, or transformation proof-of-work content."
overview: "Use Before & After when you have paired before/after photos to show. A single pair renders as a static side-by-side card with a connecting arrow; two or more pairs render as an auto-scrolling carousel track (CSS-only, no JS ticker). Ported from a proven implementation built for a dental client site, generalized with this base's own tokens and building blocks."
examples:
  - title: "Auto-scrolling Carousel (2+ pairs)"
    slugs:
      - carousel
---
