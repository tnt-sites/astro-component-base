---
title: Raw HTML
overview: "A component for rendering raw HTML content, including scripts and styles. Use this when you need to embed custom HTML that isn't available as a standard component. Note: Raw HTML bypasses Astro's built-in sanitization, so only use content from trusted sources."
examples:
---
